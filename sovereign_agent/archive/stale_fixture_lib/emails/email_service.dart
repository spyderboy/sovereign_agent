class EmailService {
  /// Sends a verification email to the specified [email] address.
  Future<void> sendVerificationEmail(String email) async {
    // Simulation of sending a verification email
    print('Sending verification email to $email');
  }
}